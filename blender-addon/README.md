Deprecated path.

The active Blender add-on source lives in `scriptony_blender_addon/`.

Use these entrypoints instead of this folder:

- `npm run addon:zip`
- `npm run addon:zip:extension`
- `npm run addon:verify`
- `scripts/build-blender-addon.sh`
